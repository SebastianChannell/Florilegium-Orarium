{
  "name": "florilegium-orarium",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node --test",
    "dev": "npm run build && node scripts/serve.mjs",
    "translations:check": "node scripts/spanish-audit.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
